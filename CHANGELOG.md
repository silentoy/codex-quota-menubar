# Changelog

## v1.4.0

- 增加“瓶颈判断方式”设置，支持默认的“按剩余百分比”和新的“按使用趋势”两种模式。
- 增加额度历史快照记录，最多保留最近 24 小时的 50 条，用于本地趋势判断。
- 新增智能瓶颈评估：按最近消耗速率预测用尽时间，优先识别早于重置前耗尽的额度窗口。
- 在历史不足或没有实际消耗时，智能模式会回退到“每 1% 需支撑时长”的静态判断。
- 为“当前瓶颈”卡片和窗口内“瓶颈”标签增加 macOS 原生悬停提示，说明当前判断依据。
- 新增 Bark 手机推送通道，支持公共 `https://api.day.app` 和自建 Bark Server。
- Bark Device Key 通过 macOS Keychain 保存，设置页支持测试消息、5 小时额度重置提醒和周额度重置提醒。
- Bark 重置通知使用独立去重记录，避免 Telegram 成功后影响 Bark 失败重试。
- Bark 通知采用纯文本标题与正文，正文包含当前剩余额度、另一个额度窗口和重置原因，适配 iOS 通知展示。
- Bark Device Key 字段支持直接粘贴完整 Bark 测试 URL，并在发送失败时显示服务端返回的具体错误原因。
- 补充瓶颈评估测试，覆盖简单模式、智能赤字、静态回退和百分比回升场景。
- 补充 Bark 请求构造测试，覆盖默认服务、自建子路径、完整测试 URL、缺配置、非法 URL 和错误响应场景。

## v1.3.0

- 优化瓶颈额度判断，支持 5 小时额度与周额度并列瓶颈。
- 将当前瓶颈提升为面板主信息，展示剩余百分比与重置时间。
- 细分 Codex 登录态读取失败原因，区分登录态缺失、token 刷新失败、HTTP 错误和接口结构变化。
- 优化面板为系统 material 层级，额度卡片、提示条和瓶颈状态更清晰。
- 为“刷新 / 打开 Codex / 设置”按钮增加系统图标，并保持 SwiftUI 原生按钮样式。
- 去掉“刷新”按钮的默认高亮状态，使面板操作按钮视觉权重保持一致。
- 设置页增加菜单栏显示预览。
- 增加 Telegram 手机推送配置，支持 5 小时额度和周额度重置提醒、测试消息、Keychain 保存 Bot Token。
- 重置通知区分到期重置、疑似服务商调整和未知恢复，并对同一重置周期去重。
- 增加 `scripts/check.sh`，打包脚本改为从参数或 `VERSION` 文件读取版本号，并补充打包自检。

## v1.2.0

- 优化设置页面排版，去掉竖向滚动条。
- 将设置页面顶部的展示图标替换为应用自身图标（AppIcon）。
- 重新设计低额度提醒控制，由滑块改为 10%/20%/30%/40%/50% 五档分段选择。
- 修复因未签名导致 `SMAppService` 开机自启设置失效的问题，更换为 LaunchAgent 方案并实现设置页面的自动状态同步。
- 升级菜单栏显示模式，支持“圆环（仅圆环）”与“百分比（仅文本）”两种格式切换。

## v1.1.0

- 顶部栏改为纯双层圆环图标，不再显示 `Codex` 或百分值。
- 外圈表示 5 小时额度，内圈表示周额度，圆环高亮部分表示剩余额度。
- 下拉面板分开展示 5 小时额度和周额度的已用、剩余、状态与重置时间。
- 构建产物增加简单双环风格 App icon。
- 失败状态展示更明确的读取或请求失败原因，并在可用时保留上次成功结果。
- 设置页增加开机启动开关。
- 增加轻量 `.app` 构建脚本 `scripts/build-app.sh`。
- README 补充 `.app` 安装步骤、成功标志和接口风险说明。

## v1.0.0

- 初始 macOS 菜单栏额度工具。
- 支持 Codex 登录态、本机状态和手动填写三种数据源。
- 支持手动刷新、自动刷新和睡眠恢复刷新。
