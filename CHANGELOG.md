# Changelog

## v1.5.2

- **修复周额度不显示**：ChatGPT usage 接口不再保证 `primary_window` 是 5 小时额度、`secondary_window` 是周额度。现在按 `limit_window_seconds` 归类窗口；当前套餐只返回周窗口时，面板会正确显示周额度，而不再误标成 5 小时。
- 套餐只返回其中一个窗口时，面板只展示有数据的额度卡片。
- **重置次数变动提醒**：Telegram / Bark 支持重置次数增加和过期减少通知，可在设置中分别开关。

## v1.5.1

- **新增“重置次数 (Reset Credits)”面板**：主面板中新增了可折叠的重置次数展示区块，支持显示当前的可用重置额度及最早可用过期时间，便于第一眼掌控额度重置节奏。
- **性能与体验优化**：修复“重置次数”与“额度消耗趋势”两个下拉抽屉在展开/折叠时导致主面板窗口剧烈抖动与闪烁的问题，改用单调平滑的过渡动画。

## v1.5.0

- 视觉需求：将主面板升级为更接近 macOS 原生 popover 的 OS27 glass 风格，统一圆角、留白、描边、阴影 and 弹性动效。
- 新增 `OS27Theme.swift`，集中管理卡片、按钮、面板的半径、间距、描边、阴影和动画参数，减少同类控件样式分散的问题。
- 重绘顶部状态区：状态徽章使用 material 背景与细描边，菜单栏额度图标增加更柔和的状态色光晕。
- 重绘“当前瓶颈”主卡片：增加独立图标块、动态 tint 玻璃背景和悬停说明，强化当前最紧急额度的第一眼识别。
- 重绘 5 小时额度和周额度卡片：状态文字前增加系统图标，进度条改为渐变填充、末端圆点和瓶颈侧边高亮。
- 重绘低额度提示、下次刷新/数据来源信息行和底部操作按钮，统一为玻璃卡片、细分隔线和 hover/press 反馈。
- 升级“额度消耗趋势”模块：支持可折叠卡片、分段 tab、图例、坐标轴和阈值线。
- 24 小时余量图展示 5 小时额度与周额度的剩余趋势，并标出当前低额度阈值。
- 7 天和 30 天消耗图改为分组柱状图，用于查看 5 小时额度与周额度的历史消耗。
- 修复 24 小时余量图在高剩余额度下出现方形异常阴影的问题，改为只绘制折线、虚线和阈值线。
- 优化本地 `.app` 打包体积：release 打包使用 `-Osize`，复制可执行文件后自动 strip，减少最终 `.app` 中的二进制体积。
- 更新 `docs/os27-mockup.html`，补充状态驱动的图表曲线、当前点、tooltip 和图表卡片展开/收起交互。
- README 补充 1.5.0 版本重点、趋势图能力和新版视觉说明。

## v1.4.1

- 补充应用的英文适配与中英文切换，默认语言自适应系统首选语言（Preferred Languages）。
- 设置页面“通用”模块中新增“语言 (Language)”切换 Picker，支持简体中文与 English 热切换，即时生效。
- 翻译面板文字、额度信息卡片、剩余与重置时间描述、智能瓶颈诊断算法的结论文字，以及 Telegram 和 Bark 的推送通知正文。
- 补充完整的英文版说明文档 `README.en.md`。
- 优化面板刷新按钮前的圆箭头图标颜色，使其动态关联最紧急的额度进度条色彩，提高警告传达敏感度。

## v1.4.0

- 增加“瓶颈判断方式”设置，支持默认的“按剩余百分比”和新的“按使用趋势”两种模式。
- 将智能瓶颈判断升级为最近 30 天小时桶聚合预测，区分周中和周末的同小时使用习惯。
- 新增本地小时频率聚合记录，只保存刷新次数、活跃次数和额度百分比下降量，不保存全量调用明细、token 或 prompt。
- 智能模式优先按未来到重置前的预计消耗判断瓶颈；无预测风险时回退到短期趋势和静态支撑判断。
- Tooltip 补充“最近 30 天周中/周末小时习惯”预测说明。
- 增加额度历史快照记录，最多保留最近 24 小时的 50 条，用于本地趋势判断。
- 新增智能瓶颈评估：按最近消耗速率预测用尽时间，优先识别早于重置前耗尽的额度窗口。
- 在历史不足或没有实际消耗时，智能模式会回退到“每 1% 需支撑时长”的静态判断。
- 为“当前瓶颈”卡片和窗口内“瓶颈”标签增加 macOS 原生悬停提示，说明当前判断依据。
- 新增 Bark 手机推送通道，支持公共 `https://api.day.app` 和自建 Bark Server。
- Bark Device Key 通过 macOS Keychain 保存，设置页支持测试消息、5 小时额度重置提醒和周额度重置提醒。
- Bark 重置通知使用独立去重记录，避免 Telegram 成功后影响 Bark 失败重试。
- Bark 通知采用纯文本标题与正文，正文包含当前剩余额度、另一个额度窗口和重置原因，适配 iOS 通知展示。
- Bark Device Key 字段支持直接粘贴完整 Bark 测试 URL，并在发送失败时显示服务端返回的具体错误原因。
- 补充瓶颈评估测试，覆盖简单模式、智能赤字、静态回退和百分比回升场景。
- 补充小时桶聚合、周中/周末预测、历史清理和智能模式回退测试。
- 补充 Bark 请求构造测试，覆盖默认服务、自建子路径、完整测试 URL、缺配置、非法 URL 和错误响应场景。
- 优化智能瓶颈评估中 5 小时与周额度的独立计算逻辑，避免周窗口因天然时间尺度优势总是霸占瓶颈。
- 支撑度分数（Support Score）引入归一化：分别除以各自固定周期常量（5h / 168h），使两窗口在相同比例尺度下公平竞争。
- 消耗流速（Burn Rate）引入时间衰减加权：基于每段中点的线性衰减，使更近期的消耗记录权重更高，提升突发消耗的响应灵敏度。
- 重置前用尽缺口（Deficit）改用 timeToDrainHours 排序：多窗口同时面临用尽风险时，优先选择最先归零的窗口。
- 习惯预测超额风险时为 5 小时窗口引入 1.5 倍 riskRatio 加权，平衡短周期窗口容错率更低的特性。
- 两窗口数据完全相同时（单一数据源退化场景）默认选择 5 小时额度作为瓶颈，避免界面显示含义不明的"并列瓶颈"。
- 新增额度瓶颈计算规则设计文档 `docs/quota_bottleneck_rules.md`，包含完整的计算规则详解和优化方案。

## v1.3.0

- 优化瓶颈额度判断，支持 5 小时额度与周额度并列瓶颈。
- 将当前瓶颈提升为面板主信息，展示剩余百分比与重置时间。
- 细分 Codex 登录态读取失败原因，区分登录态缺失、token 刷新失败、HTTP 错误和接口结构变化。
- 优化面板为系统 material 层级，额度卡片、提示条和瓶颈状态更清晰。
- 为“刷新 / 打开 Codex / 设置”按钮增加系统图标，并保持 SwiftUI 原生按钮样式。
- 去掉“刷新”按钮的默认高亮状态，使面板操作按钮视觉权重保持一致。
- 设置页增加菜单栏显示预览。
- 增加 Telegram 手机推送配置，支持 5 小时额度和周额度重置提醒、测试消息、Keychain 保存 Bot Token。
- 重置通知区分到期重置、疑似服务商调整和未知恢复，并对同一重置周期去重。
- 增加 `scripts/check.sh`，打包脚本改为从参数或 `VERSION` 文件读取版本号，并补充打包自检。

## v1.2.0

- 优化设置页面排版，去掉竖向滚动条。
- 将设置页面顶部的展示图标替换为应用自身图标（AppIcon）。
- 重新设计低额度提醒控制，由滑块改为 10%/20%/30%/40%/50% 五档分段选择。
- 修复因未签名导致 `SMAppService` 开机自启设置失效的问题，更换为 LaunchAgent 方案并实现设置页面的自动状态同步。
- 升级菜单栏显示模式，支持“圆环（仅圆环）”与“百分比（仅文本）”两种格式切换。

## v1.1.0

- 顶部栏改为纯双层圆环图标，不再显示 `Codex` 或百分值。
- 外圈表示 5 小时额度，内圈表示周额度，圆环高亮部分表示剩余额度。
- 下拉面板分开展示 5 小时额度和周额度的已用、剩余、状态与重置时间。
- 构建产物增加简单双环风格 App icon。
- 失败状态展示更明确的读取或请求失败原因，并在可用时保留上次成功结果。
- 设置页增加开机启动开关。
- 增加轻量 `.app` 构建脚本 `scripts/build-app.sh`。
- README 补充 `.app` 安装步骤、成功标志和接口风险说明。

## v1.0.0

- 初始 macOS 菜单栏额度工具。
- 支持 Codex 登录态、本机状态和手动填写三种数据源。
- 支持手动刷新、自动刷新和睡眠恢复刷新。
